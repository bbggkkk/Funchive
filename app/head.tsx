export default function Head(){
    return (
        <head>
            <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover"/>
            <title>Funchive</title>
            <style>{`*{font-family:var(--default-font), sans-serif;}`}</style>
        </head>
    )
}